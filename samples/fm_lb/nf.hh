#ifndef NF_HH
#define NF_HH

#include "service_chain.hh"

using NF = serviceChain;                         // specify NF class
using nf_flow_state = chain_flow_state;   // specify NF state class

#endif